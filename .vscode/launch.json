{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chromium",
            "request": "launch",
            "name": "Iniciar o Chrome em relação a localhost",
            "url": "http://localhost:3333",
            "webRoot": "${workspaceFolder}",
        }
    ]
}